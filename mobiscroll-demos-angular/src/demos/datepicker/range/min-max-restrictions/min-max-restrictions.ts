import { Component } from '@angular/core';
import { setOptions /* localeImport */ } from '@mobiscroll/angular';

setOptions({
  // locale,
  // theme
});

@Component({
  selector: 'app-range-min-max-restrictions',
  templateUrl: './min-max-restrictions.html',
  standalone: false,
})
export class AppComponent {}

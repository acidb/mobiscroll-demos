import { Component } from '@angular/core';
import { setOptions } from '@mobiscroll/angular';

setOptions({
  // locale,
  // theme
});

@Component({
  selector: 'app-popup-event-creation-popover',
  templateUrl: './event-creation-popover.html',
})
export class AppComponent {}
